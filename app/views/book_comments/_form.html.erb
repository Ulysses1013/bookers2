<%= form_with model: [book, book_comment], local: false do |f| %>

  <div class="form-group">
    <%= f.text_area :comment, rows: '4' , class: 'form-control border border-dark' %>
  </div>

  <div><%= f.submit "送信" %></div>

<% end %>